'use client';

import Link from 'next/link';
import styles from './Navbar.module.css';

const Navbar = () => {
    return (
        <div classname={logo}>
            <span>ICON</span>
            <span>TEXT</span>
        </div>

    );
};

  export default Navbar;